include header.pug

h1 Add New Product
form(method='POST' action='/Add-Product')
  div
    label(for='name') Name: 
    input#name(type='text', name='name')
  div
    label(for='quantity') Quantity: 
    input#price(type='text', name='quantity')
  button(type='submit') Add

include footer
